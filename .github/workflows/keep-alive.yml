name: Keep Render backend warm

on:
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: GET /api/health
        run: curl -fsS --max-time 120 --retry 3 --retry-delay 30 --retry-all-errors https://deetechcomputers-com.onrender.com/api/health
